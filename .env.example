#Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=namu
DB_PASSWORD=namu123
DB_NAME=namu_ai

# LLM Configuration
# Use uma API na nuvem (OpenAI, Anthropic) ou um modelo local via Ollama
LLM_MODEL=llama3
OLLAMA_BASE_URL=http://localhost:11434

# Exemplo de configuração para ambiente local
# Copie este arquivo para .env antes de rodar o projeto